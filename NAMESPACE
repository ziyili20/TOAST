import( stats )
import( methods )
import( SummarizedExperiment )
import( RefFreeEWAS )
import( EpiDISH )
import( limma )
import( nnls )
import( corpcor )
export(
    Tsisal,
    assignCellType,
    csDeconv,
    csTest,
    DEVarSelect,
    findRefinx,
    fitModel,
    makeDesign,
    ChooseMarker,
    GetPrior,
    MDeconv
)
